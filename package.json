{
  "name": "saas-multi-tenant-mern",
  "version": "1.0.0",
  "description": "Système SaaS Multi-tenant pour la gestion des communications Outlook et WhatsApp avec analyse IA",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "saas",
    "multi-tenant",
    "mern",
    "communications",
    "ai",
    "whatsapp",
    "outlook"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
